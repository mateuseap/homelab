# Plain-text secrets must never land in git, only sealed-secrets.yaml is safe.
secrets.yaml
*.secret.yaml
sealing-key-backup.yaml
*.sql.gz

# Local Claude worktrees are per-machine state, never committed.
.claude/worktrees/
